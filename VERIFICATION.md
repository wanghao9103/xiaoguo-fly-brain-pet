# 验证记录

## 发布基线

- 日期：2026-09-22。
- 环境：Windows 10、Python 3.14、Tk 8.6。
- python -m unittest discover -v：121 项通过；随后新增桌宠棋局事件隔离检查单独通过，共 122 项。
- 测试使用合成模型、克隆或临时目录，不读取正式宠物存档。

## 自动检查范围

| 层次 | 验证内容 |
| --- | --- |
| 学习核心 | 稀疏性、归一化、正负反馈、情境学习、反转、权重边界 |
| 模型与存档 | JSON 往返、RNG 继续序列、非法输入、损坏备份恢复、替换失败、单实例锁 |
| 交互 | 单击/双击分流、反馈快照、取消回调、拖动不奖励、游戏计时与暂停 |
| 情绪 | 阶段边界、只读派生、安抚恢复、暂停与离线时间、眼泪动画计时 |
| 决策证据 | 概率与实际动作一致、读取不改变 RNG、来源标记 |
| 实验计算 | 固定种子、互补扰动、冻结零更新、等预算对照、输入模型不变 |
| 实验服务 | 手动/自动模型隔离、本机 API 边界、不可变起点、报告实际落盘 |
| 棋局规则 | 五子棋四方向/长连/防守，象棋44开局合法着、将死/困毙、将军、马腿/象眼/炮架/照面、重复和棋 |
| 棋局搜索 | 战术优先、候选合法、只读搜索、两层吃子静态扩展修复炮换马截断误判 |
| 棋步学习 | 稀疏批训练误差、反事实训练改变选招、关闭学习零更新、终局只反馈一次、两棋种权重隔离 |
| 棋桌会话 | 过期版本拒绝、保存失败回滚、先后手/悔棋、重开不发奖励、关闭后不可写盘、桌宠事件不奖励日常行为 |

## 交互验收

学习实验室已通过真实浏览器操作与截图检查：

- 正负反馈改变行动倾向，改变滑块不增加学习次数。
- 五项实验能显示曲线、表格和限制说明。
- 继续训练后，旧结果正确标为历史。
- 保存按钮生成可读取的 JSON，包含起点与当前状态。

桌面原型已在 Windows 启动；拖动、暂停、冻结、保存和动画边界经过组件检查。角色默认 208×208，面板支持滚动。

## 棋桌交互验收

- 使用独立 qa/chess-session 存档，真实浏览器验证五子棋落子/回棋/悔棋，象棋选中、合法目标、红黑方向切换、回棋及认输。
- 示范训练未移动棋盘，实际五子棋批误差 0.5656 → 0.3766，样本训练次数 12 → 72。
- 象棋认输后终局样本数变为 1，完整棋谱计数变为 1；关闭学习后显示纯搜索，记忆评分前后相同。
- 修复棋子被悬停样式覆盖的颜色问题，以及纯搜索时说明误称神经评分参与选择的问题；最终截图确认黑白子与末步金圈、象棋棋子与绿色目标清楚。
- 390px 视口下读取到棋盘宽度 309px、文档内容宽度 375px，无横向溢出。桌面截图确认布局；浏览器控制台未见错误或警告。
- Python 3.14 环境，两层象棋搜索含静态扩展在开局红兵一步后约 1.38 秒；局面不同会有变化。
- 测试使用隔离存档，未用测试数据修改正式日常行为存档。新增桌宠菜单与事件处理有组件测试，原生窗口联动未作新的人工点击验收。

## 限制

- 原生桌面截图工具遇到接口兼容问题；没有把组件检查称为完整的原生视觉或鼠标端到端验收。
- 未验证多显示器漫游、Mac/Linux 透明窗口、长期学习稳定性、语音或自然图片理解。
- 合成实验不证明一般智能或主观意识。
- 棋步评分误差与选招改变不证明棋力提升；未进行 Elo、长期自我对弈或正式比赛规则验收。
- 可运行 python verify_runtime.py 在桌面会话中生成 qa/verification.json；qa/ 和个人实验报告不随仓库发布。

## 复现命令

~~~powershell
python -m unittest discover -v
python verify_learning.py
python verify_runtime.py
~~~

最后一项需要图形桌面。自动 CI 仅运行无窗口测试和合成学习验证。
